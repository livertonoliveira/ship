# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/settings.local.json

# Ship local artifacts (Linear mode — never commit)
ship/changes/

# Ship pipeline state (trace runs, active-run pointer — generated artifacts)
ship/state/

# Ship temporary pipeline artifacts
ship/tmp/
ship/.tmp/

# Ship run-context scratch (shared between pipeline agents — never commit)
.context/ship-run/

# Ship work-graph state (node claims, footprints, merge scratch — generated)
.context/ship-graph/

# Ship audit inventory (regenerated per audit run — derived from git ls-files)
.context/ship-audit/

# Ship plugin source lives in src/ (committed). Built output is
# plugins/ship/skills/ — also committed, since the marketplace ships
# whatever is in git. Run `npm run build` in plugins/ship/ after
# editing anything under src/.
